#include<stdio.h>
#include<type.c>
int main(){
    int ch;
    scanf("%d %d",&cap,&lil);
    if(isupper(ch)){
        printf("Uppercase");
    }
    else if(islower(ch)){
        printf("Lowercase");
    }
}